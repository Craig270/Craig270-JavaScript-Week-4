"use strict";

//a function that takes firstName and lastName and returns ‘fistName lastName’
function nameMaker(firstName, lastName) {
  console.log(`${firstName} ${lastName}`);
  return `${firstName} ${lastName}`;
}

nameMaker("Craig", "Neff");
